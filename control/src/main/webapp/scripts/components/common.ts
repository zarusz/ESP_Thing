module App {

    export class NgSvc {
        static rootScope = "$rootScope";
        static scope = "$scope";
        static http = "$http";
        static state = "$state";
        static $q = "$q";
    }
}